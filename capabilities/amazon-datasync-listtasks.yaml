naftiko: 1.0.0-alpha2
info:
  label: 'Amazon DataSync REST API — #ListTasks'
  description: 'Amazon DataSync REST API — #ListTasks. 1 operations. Lead operation: Amazon DataSync List Tasks. Self-contained
    Naftiko capability covering one Amazon Datasync business surface.'
  tags:
  - Amazon Datasync
  - '#ListTasks'
  created: '2026-05-19'
  modified: '2026-05-19'
binds:
- namespace: env
  keys:
    AMAZON_DATASYNC_API_KEY: AMAZON_DATASYNC_API_KEY
capability:
  consumes:
  - type: http
    namespace: amazon-datasync-listtasks
    baseUri: https://datasync.{region}.amazonaws.com
    description: 'Amazon DataSync REST API — #ListTasks business capability. Self-contained, no shared references.'
    resources:
    - name: '#ListTasks'
      path: /#ListTasks
      operations:
      - name: listtasks
        method: POST
        description: Amazon DataSync List Tasks
        outputRawFormat: json
        outputParameters:
        - name: result
          type: object
          value: $.
        inputParameters:
        - name: body
          in: body
          type: object
          description: Request body (JSON).
          required: false
    authentication:
      type: apikey
      key: Authorization
      value: '{{env.AMAZON_DATASYNC_API_KEY}}'
      placement: header
  exposes:
  - type: rest
    namespace: amazon-datasync-listtasks-rest
    port: 8080
    description: 'REST adapter for Amazon DataSync REST API — #ListTasks. One Spectral-compliant resource per consumed operation,
      prefixed with /v1.'
    resources:
    - path: /v1/listtasks
      name: listtasks
      description: 'REST surface for #ListTasks.'
      operations:
      - method: POST
        name: listtasks
        description: Amazon DataSync List Tasks
        call: amazon-datasync-listtasks.listtasks
        with:
          body: rest.body
        outputParameters:
        - type: object
          mapping: $.
  - type: mcp
    namespace: amazon-datasync-listtasks-mcp
    port: 9090
    transport: http
    description: 'MCP adapter for Amazon DataSync REST API — #ListTasks. One tool per consumed operation, routed inline through
      this capability''s consumes block.'
    tools:
    - name: amazon-datasync-list-tasks
      description: Amazon DataSync List Tasks
      hints:
        readOnly: true
        destructive: false
        idempotent: false
      call: amazon-datasync-listtasks.listtasks
      with:
        body: tools.body
      outputParameters:
      - type: object
        mapping: $.
